import React from 'react';

const DifferenceDisplay = ({ difference }) => {
    return (
        <div>
            <h2>Difference:</h2>
            <p>{difference}</p>
        </div>
    );
};

export default DifferenceDisplay;
